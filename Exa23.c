main()
{
	int number;
	printf("Enter an integer number :\n");
	scanf("%d",&number);
	
	if (number < 100)
	 printf("your number is smaller than 100.\n");
	else
	 printf("your number is contain more than two digits.");
}
